# Handoff — menhir session 2026-07-07

Single location: `projects/archolith/menhir` on `main`. Tree clean, all pushed. Server live on :8090.

## What shipped this session (in order)
1. **Experience-counter fold redesign** (`c87578d`) — instability fold excluded `scope` lifecycle
   churn (live 26,557 -> 4 counters) + skip embedding unchanged counters. Purged 5,029 stale
   `scope_revised` counters from Neo4j. Re-enabled the job; verified `{failure:20, instability:4}`, ~7s.
2. **merge_entity Cypher fix** (`796ec9d`) — the correlation/dedup merge path was fully dead
   (`CypherSyntaxError: Invalid input '|'` + nonexistent `json()`); rewrote two-phase with a
   Python-serialized audit entry. Both queries EXPLAIN-clean against live Neo4j.
3. **Scheduler lease self-healing** (`09f6b10`) — Windows restart hard-kills the old server
   (TerminateProcess), so the lease was never released and every restart needed a manual
   `force_scheduler_takeover`. `try_acquire` now reclaims a still-in-TTL lease from a **dead same-host
   PID** (`_pid_alive`: POSIX `os.kill(pid,0)`; Windows `OpenProcess`+`GetExitCodeProcess`). Restarts
   are self-healing now — every restart since shows `forced_takeover: None`.
4. **Graph-native verifiers** (`decbecd`, `1ccc230`) — keep derivable beliefs fresh vs their source
   of truth. A `(:Entity {is_verifier})` node holds a binding (kind+params+register), executor lives
   in a trusted in-code registry (`env_key`), unknown kinds skipped. Register self-supersedes via
   `record_counter`; `(register)-[:VERIFIED_BY]->(verifier)`, `(belief)-[:REFERENCES]->(register)`.
   Wired as opt-in scheduler job (`MENHIR_VERIFIER_SYNC_ENABLED`, on here, 300s). **Live**: 3 verifiers
   seeded (experience_counter_enabled, structure_watcher_enabled, api_port), self-heals on drift.
5. **Phase 3: personal-memory consolidation** (`e62ae0b`, `b4af23d`) — the gated perception boundary
   (`perceive_and_fold`) that folds count/amount Views ("bike spend", "fish tanks owned") from user
   turns now runs automatically. See below.

## Phase 3 — what to know
- **Why not on-ingest:** the fold is k-sample LLM (~6 calls/fact); on-ingest would block the write
  path and re-fold on every message. Instead: ingest cheaply marks a namespace dirty; a **short-interval
  background pass (300s)** re-folds only dirty namespaces. Near-real-time without the hot-path cost.
- **Guards pinned ON** (decision 5): cross-check + coref + verify all forced on, so an automatic write
  never commits a confident-but-wrong total. **Decision 6** (the plan's stated pre-req, Law-3 blind
  spot) was verified **already implemented** in the gate — no new gate work was needed.
- **Files:** `infrastructure/personal_memory_queries.py` (dirty query + user-episode load +
  `ConsolidationWatermark` debounce), `services/scheduler_tasks.consolidate_personal_memory`,
  `infrastructure/sync_llm.make_sync_chat` (sync OpenAI-compatible seam), adapter delegators.
- **Toggle:** `MENHIR_PERSONAL_MEMORY_CONSOLIDATION_ENABLED` (on here), `_INTERVAL_S=300`, `_K=3`,
  `_CALL_BUDGET=300`. Job registers only when enabled AND a chat provider resolves.
- **Live state:** registered (10 scheduler jobs), ran clean `{namespaces_dirty:0, ...}`. **It found 0
  namespaces to fold — expected: this prod box has no `user:`-turn Episodic nodes** (the LME eval data
  lived in a separate Neo4j on :7689). So the job is wired and proven to run, but has not folded a real
  View here yet. To see it actually work, point it at a graph that has user turns, or ingest some.

## Live state (verified)
- Server UP :8090 (`/api/ready`=200). Scheduler running, lease held by live pid, `forced_takeover: None`.
- **10 jobs**: the 6 core + refresh_structure_graphs + sync_experience_counters + sync_verifiers +
  consolidate_personal_memory.
- `.env` non-default toggles: `MENHIR_EXPERIENCE_COUNTER_ENABLED=true`, `MENHIR_VERIFIER_SYNC_ENABLED=true`,
  `MENHIR_PERSONAL_MEMORY_CONSOLIDATION_ENABLED=true`; `LANGFUSE_*` commented out.

## Restart / ops
- `pwsh scripts/start-server.ps1 -Action restart`. Restarts are self-healing (`09f6b10`); no manual
  lease step needed. If ever `running:False` from a *live* stray owner, call `force_scheduler_takeover`.
- Health `GET /api/ready`; jobs `GET /api/stats` with `Authorization: Bearer <MENHIR_OPERATOR_KEY>`.
- Telemetry DB `C:\Users\you\IdeaProjects\.agent\mcp_telemetry.db`; Neo4j `bolt://neo4j.example.internal:7687`.

## Owed / not done
- **Phase 3 real-data validation** — the job has never folded a live View on this box (no user turns).
  Next: ingest/point at a graph with `user:` Episodic turns; confirm Views written, re-run idempotent
  (watermark debounces), a new turn re-dirties. Plan verification #2/#3 in
  `.agent/plans/perception-consolidation-prod-wiring.md`.
- **Verifier follow-ups** (`.agent/plans/graph-verifiers.md`): more executor kinds
  (`http_status_field`, `file_fingerprint`), string-valued registers, recall down-ranking of
  `needs_review` prose.
- **Capstone benchmark re-run** (merge Phase-0 evidence) — needs live services.
- `id()` -> `elementId()` deprecation in merge_entity Cypher (warning only).

## Key commits (main)
`b4af23d` personal-memory job wiring · `e62ae0b` personal-memory core + dirty queries ·
`1ccc230` verifier job wiring · `decbecd` verifier prototype · `09f6b10` lease self-heal ·
`796ec9d` merge_entity Cypher fix · `c87578d` experience-counter fold redesign.
