Follow AGENTS.md for repository-wide rules. Start with .agent/README.md and use its task router; do not
preload the whole .agent directory. Use Menhir structural queries before filesystem search, check blast
radius before edits, preserve unrelated worktree changes, and run focused affected tests.
